# Application Environment
NODE_ENV=development

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Configuration (Required)
# Get these from your Supabase project settings: https://app.supabase.io/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (Backend only - NEVER expose to client!)
# Required for admin operations and migrations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Direct Connection (For migrations and admin tasks)
# Format: postgresql://postgres:[password]@[host]:[port]/postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Optional: Anthropic API (for AI features if implemented)
# ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: Error Reporting Service
# SENTRY_DSN=your-sentry-dsn
# SENTRY_ORG=your-org
# SENTRY_PROJECT=your-project

# Optional: Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Development Tools
# Supabase Studio URL (local development)
SUPABASE_STUDIO_URL=http://localhost:54323

# Testing
# Used for running tests against local Supabase
TEST_SUPABASE_URL=http://localhost:54321
TEST_SUPABASE_ANON_KEY=your-test-anon-key
TEST_SUPABASE_SERVICE_KEY=your-test-service-key

# GitHub Actions Secrets (Add these to your GitHub repository settings)
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY (for production deployments only)
# - DATABASE_URL (for running migrations in CI/CD)